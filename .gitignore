# macOS
.DS_Store

# Hugo generated files
public/
resources/_gen/
.hugo_build.lock

# Node
node_modules/
npm-debug.log
package-lock.json

# Editor
*.swp
*.swo
*~

# Temporary files
*.tmp
.cache/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# OODA agent scratch (traces, briefs, body-tmp, tool stdout buffers)
.ooda/

# Unpublished manuscripts. Kept local by choice, not tracked anywhere.
# Nothing here is recoverable from git — back it up outside the repo.
content/book/
